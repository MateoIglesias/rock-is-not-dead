import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Rock Is Not Dead',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
